# V24.5 S1.7 — ID PREFLIGHT RUNTIME VALIDATION

**Status:** PASS / FUNCTIONALLY COMPLETE
**Date:** 2026-09-14

## Runtime evidence

Browser runtime validation confirms:

1. Existing package/map ID is detected before durable import commit.
2. Duplicate import is rejected with `ID_COLLISION`.
3. Progress reaches 100% immediately on the rejection path; the UI does not appear stuck.
4. User-facing warning is concise: `Ditolak — ID peta sudah ada di Library.`
5. Lithosite toast is displayed for restore rejection/success.
6. `committedCount: 0` is reported for the collision path.
7. Existing active map remains intact; no overwrite or accidental activation occurs.
8. A non-colliding backup package imports normally and restores into Map Library.
9. Export of the selected map succeeds and reports SHA-256 integrity metadata.
10. Restored map is rendered successfully after restore.

## Console classification

The S1.7 runtime screenshots still show the pre-existing CRS/config fallback warning (`AbortError`) from the server configuration path. This is outside the S1.7 package-transfer boundary and is not introduced by the ID preflight implementation.

`Console Clean` is therefore **not claimed**.

## Decision

S1.7 ID preflight/collision guard is accepted and locked as a functional V24.5 gate.
